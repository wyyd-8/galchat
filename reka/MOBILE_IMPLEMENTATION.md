# 手机端实现与验收记录

## 2026-09-11：人物卡创建流程的手机视觉适配

- 建卡入口保留三种方式的纵向布局，移除桌面阴影、彩色顶线与悬浮位移；统一标题、编号、说明和提示条的手机尺寸。
- 修正建卡内容区被桌面样式覆盖的左侧留白。AI 页面取消桌面面板底色和最小高度，生成说明、来源与骰点提示采用手机字号及间距，沿用固定底部操作。
- 导入页使用统一底色，文本框随视口高度调整；导入说明纵向排布，属性识别保留两列，窄屏内容正常换行。
- 全部改动限定在 767px 以下的媒体查询，生成、识别、草稿及绑定逻辑未变。

验证：Chrome 用真实组件及隔离数据核对 390px 建卡入口、AI 与导入页，以及 320px 入口、文本输入、识别结果滚动和固定按钮。导入完整身份与八项属性后可识别，返回再进入仍保留原文。16 项建卡文案、导入解析及绑定流程测试通过；类型检查、生产构建与 diff 检查通过，构建仍有既有大分包提示。未调用实际生成或绑定接口；临时预览已清理。

## 2026-09-11：手机细节修正

- 跑团总结的检定点数列留出三位数宽度并禁止换行，100 保持完整单行。
- 普通群聊参与角色改为弹性行，姓名占剩余空间，勾选框统一靠右。
- 世界对话中心上方并列“添加角色 / 新建群聊/跑团”，移除列表底部的添加入口。
- 手机主持人设定仅渲染当前选中字段，切换后标签与正文同步更新；桌面保留全部字段。
- 手机素材详情取消图片操作遮罩与替换，删除素材移至固定底部的整行按钮；只读及受限编辑保持无删除权限。
- 模组“可用于新建跑团 / 重要线索”开关补充左右 14px 内边距。
- 手机登录页去除重复头部标识，保留可访问标题与正文品牌标识。

验证：模组布局、跑团总结和手机聊天的 30 项测试通过；类型检查、生产构建和 `git diff --check` 通过。Chrome 实际登录页核对标识，其余使用真实组件与隔离测试数据逐项核对，包括特殊规则切换、素材底部删除布局、两个开关、右侧勾选及勾选交互、并列创建入口和 100 单行。未修改真实账户数据；临时预览文件与标签页已清理。桌面素材操作及主持人全部字段的回归测试通过；构建保留既有大分包提示。

## 2026-09-11：场景角色的回复模型弹窗

- 手机场景角色状态下方的“打开人物卡”替换为“切换回复模型”，增加独立 foreground 底部弹窗，保留场景页和展开位置。桌面人物卡入口保留。
- 纸色背景与松绿单选卡片，展示角色、当前模型、自定义模型名、实际模型名及连接状态；列表滚动，取消与确认固定底部。
- 默认模型、已删除模型回退及空列表均有说明；玩家本人禁用此入口并说明由本人输入。NPC 映射到 KP 的运行设置并提示共用范围；AI 调查员按回复角色 ID 保存，保留原有控制方式。
- 确认前不提交；保存时防重复提交，失败保留选择并允许重试，成功返回原场景。接入现有运行设置保存接口。

验证：27 项手机聊天、模型目标与保存参数、弹窗历史及角色控制测试通过；类型检查、生产构建和 `git diff --check` 通过。Chrome 在真实调试跑团核对 NPC 入口、真实模型列表和 KP 共用提示，未提交真实配置；独立本地交互预览验证取消无请求、重新打开恢复当前选择、失败保留选项、重试成功关闭并返回父页，预览已清理。构建仍有既有的大分包提示。

## 2026-09-10：场景与队伍内部重排

范围：仅手机宽度的“场景与队伍”内容区。探索与战斗共用页面和视觉规则，取消切换与类型筛选；桌面侧栏和执行状态数据逻辑保留。

- 场景摘要采用浅松绿纸色、紧凑标题、时间入口与独立进度提示；行动轮之间仍展示已有场景名称。
- 行动顺序采用整宽列表，头像、姓名与文字状态形成稳定的三列结构。等待输入、投骰或执行中的角色以统一浅绿色整行提示。
- 临时响应保留与发起角色的关系，但去除层层缩进；关联场景、暂不参与和已完成分组保持可读。人物卡详情原地展开，属性为双列。
- 修正桌面状态选择器后加载时覆盖手机网格和阴影的问题；去掉底部重复的“当前计划”文案，保留人物卡与控制入口及原有操作。

验证：Chrome 实际防守响应场景核对整行高亮、展开详情和 320px 窄屏；点击人物卡入口后应用返回登录页，该跳转未完成验证。17 项手机聊天、角色控制与场景执行测试通过，新增用例覆盖探索和战斗均直接展示队伍及临时响应关系；类型检查与生产构建通过，`git diff --check` 通过。仍有既有的大分包提示。

## 2026-09-10：世界存档预览层级与消息分割线

- Chrome 复现世界存档预览的假死：确认页已经接管焦点，但同层级的世界存档目录仍覆盖在画面上。世界读取确认和保存编辑页在手机宽度使用既有 foreground 层级，取消或返回后保留原目录；桌面层级不变。
- 手机端单聊、群聊及跑团消息间添加 1px 浅灰居中分割线，按后续调整占消息区域的 80%，两侧留白。最后一条消息后不显示，单聊的思考过程仍与对应回复保持在同一侧。

验证：Chrome 使用已有世界存档复现并核对修复后的打开、取消、再次打开、顶部返回及覆盖编辑页，未确认读取或覆盖；核对单聊中短线的位置。19 项弹窗历史与手机聊天相关测试通过，类型检查与生产构建通过。构建仍有既有的大分包提示。

## 2026-09-10：存读档、掷骰记录与实际骰窗校正

本轮继续对照第一版 `restore` / `trpg-save` / `dice-history` 以及星期六 19:39 的 dice-v4 修正以下偏差：

- 恢复预览新增独立手机结构：目标时间与影响提示、恢复边界消息、删除分界、调查员快照。长边界正文默认六行并可展开全文，其余上下文也保留；不再套用桌面预览框和侧栏。自动回退点恢复为箭头目录行。
- 掷骰记录新增独立手机结构：纵向筛选、日期分组、“角色 · 检定”标题、醒目骰点/阈值、查看回放与定位。完整行动原因可展开，多人分别显示真实点数与成功等级，缺失数据不虚构。
- 实际骰窗修复 CSS 级联：隔离后加载的桌面皮肤和共用弹窗规则，恢复 v4 纸色窗口、24px 标题、10px 骰子标签及两列骰盘；骰子视口上限188px，组下结果与动作底栏分开。未改3D模型、音频、核心旋转动画或现有材质参数；同时开掷及直接聚焦组下结果的调度保留。

验证：`npm test` 474/474 通过；`npm run build` 类型检查与生产构建通过；`git diff --check` 通过。新增 CSS 级联回归测试覆盖320/390/767px与四种骰面，弱选择器负对照可复现旧皮肤和标题覆盖。Chrome在实际应用手机布局中核对了存档/恢复预览、记录标题和筛选，并播放一条真实历史检定，确认播放中与完成结果的窗口表现。没有执行真实恢复、覆盖存档或生成新的检定。多人/跨组/特效的既有调度由代码核对与相关测试验证，本轮未在实体手机上逐项重播这些情形。构建仍有现有大于500 kB的分包提示。


## 2026-09-10：按第一版重做手机窗口

本轮以最初的 `galchat-mobile-design.html` / `design-spec.md` 为视觉依据，并将原稿 87 个页面提取到设计目录 `v1-reference/` 逐项映射。后续新增功能沿用第一版的页面与表单规则。掷骰保留星期六约 19:39 确认的 dice-v4，本轮未修改掷骰文件。

界面按窗口宽度切换：不超过 767px 使用手机结构，从 768px 起保留桌面结构。调整的是页面层级、窗口、滚动、导航与组件排列，不只是字号。

| 模块 | 本轮重做内容 |
| --- | --- |
| 共用窗口 | 64px 顶栏、左侧逐层返回、单列正文、固定操作栏；短操作使用底部弹层；第一版纸色、松绿、边线、字号和间距；系统返回处理子页面 |
| 世界 | 世界首页的首张封面与后续列表、双列模板；世界详情封面、存档入口、会话列表、底部导航；创建、模板预览、角色添加和世界设置独立页 |
| 单聊与群聊 | 第一版正文阅读排版、用户气泡、输入栏；角色详情与记忆表单；群聊排序、跑团场景、行动轮设置与结束状态独立视图 |
| 模组 | 首页、六项目录、基本资料、主持人设定、地点、线索、素材和角色卡；列表进入独立详情；导出/解锁收进主目录的更多操作；受限编辑提示始终固定铺满底部 |
| 人物卡与建卡 | 调查员列表、资源 2×2、属性 4×2、技能与武器正文；建卡方式、六步流程、背景和装备详情、导入审阅；恢复已有草稿也使用全屏正文和固定底栏 |
| 工具与报告 | 六行工具目录、角色运行设置、存档与恢复预览、武器风险底层窗口、导出；结团报告五章与固定翻页 |
| 账号与模型 | 我的页面、登录账号窗口、模型列表到编辑、独立 cURL 导入、测试结果；与角色模板、好感阶段相关的表单与详情 |

### 最终验证

- `npm test`：464 项通过，0 失败。
- `npm run build`：类型检查与生产构建通过；仍有现有的超过 500 kB 分包提示。
- `git diff --check`：通过。
- 使用 Chrome 的真实应用页面 `localhost:5173` 和现有数据检查；未为视觉验收提交新的世界、会话、模型或人物卡。
- 390px：世界首页和详情、单聊与角色详情、角色模板/好感阶段、世界设置/世界书、模型列表/编辑/cURL、模组目录/素材详情、跑团工具/场景/行动轮设置、人物卡/武器风险、已有建卡草稿。
- 320px：创建会话、模组选择、参与者及同行档案、绑定列表、人物卡资源/属性、存档和恢复预览、结团报告及章节翻页。修复了调查员列表 210px 内滚动上限和模组选择行的半宽问题。
- 1280px：确认建卡窗口恢复桌面的调查员侧栏、主编辑区及右侧预览；未套用手机窗口。
- 排除热更新旧组件状态：整页刷新后再次检查模组素材详情，确认标题取当前素材名称、重复列表行已移除。
- 建卡回归检查同时清除了背景 480px 与武器 260px 内部滚动上限，仅正文滚动，下一步固定页底。

### 实现与验收边界

原稿里部分交互与现有接口不同：建卡仍使用现有统一技能预算和单向步骤提交；公开模板角色预览只提供接口允许的名称与头像；世界设置只编辑服务支持的字段。未添加没有保存能力的控件或虚构数据。

本轮覆盖了上述真实页面与关键状态，未逐一执行 87 个设计状态的全部业务流程，也未重新生成、绑定整张人物卡或执行实际存档覆盖、付费模型调用、PDF 下载。尚未在实体 iOS/Android 设备上验收软键盘、安全区与长时间使用。桌面复杂业务交互未全部重跑。

---

## 此前实现记录（保留历史，本轮结果见上）

2026-09-10。本轮范围为整套手机方案，包含原有世界、聊天和掷骰，以及后续新增视图。前端改动尚未提交。

## 已接入的模块

| 模块 | 本轮实现 |
| --- | --- |
| 导航与共用布局 | 767px 手机断点；世界、模组、我的三入口；详情页隐藏底部主导航；长表单全屏、短操作底部弹层；安全区、可视视口和触控尺寸 |
| 世界与角色 | 世界列表、对话中心、模板与设置表单；读档确认的角色好感快照；同伴列表与独立详情、已选队伍收起 |
| 聊天 | 私聊、群聊与跑团；手机回车换行；按世界和对象保留草稿；阅读位置恢复、回到最新；场景、队伍、模型与排序入口；图片查看与缩放 |
| 掷骰 | 手机独立窗口；沿用生产骰子模型、材质、核心动画与音频；每行最多两颗、组内换行；同时开掷；原底部信息迁入对应组下，跨组在末组显示；直接滚到结果；暂停跟随、重放及继续 |
| 人物卡与工具 | 调查员列表到单人详情；三种建卡入口；文本导入、独立模板说明、导入草稿；步进目录与预览；技能筛选排序、武器信息；运行设置、完整恢复预览；PDF 导出弹层 |
| 结团报告 | 五章目录、独立正文滚动、固定翻页操作；手机封面；人物单列；统计对象选择和双列计数 |
| 模组 | 模组列表到目录，再到章节与单项编辑；保存、返回和未保存保护；受限内容保存基线；角色编辑全屏 |
| 账号与模型 | 我的页面；账号、皮肤、密码入口；模型列表和编辑；独立 cURL 导入；密钥显示控制；未保存关闭确认 |
| 弹层返回 | 手机系统返回逐层关闭；恢复焦点；异步关闭可被未保存守卫拒绝；确认框关闭后保持原编辑层 |

## 已完成验证

- `npm test`：450 项通过，0 失败。
- `npm run build`：包含 `vue-tsc --noEmit`，通过。保留已有的超过 500 kB 分包提示。
- `git diff --check`：通过。
- 使用真实 Vue 组件与独立示例数据，未向生产代码加入模拟路由。
- 320px：世界、聊天、模组单项编辑、人物卡、报告和骰窗检查；检查过的页面未出现横向溢出。
- 390px：世界详情、创建跑团、同伴选择、场景与工具入口。
- 767px：模组列表与手机导航断点；完整模型列表和窗口关闭。
- 1280px：主页面保留桌面侧栏，手机导航隐藏，无横向溢出。
- 掷骰实际播放：三组同时处于 rolling；双奖励骰四颗拆为两行；末组结果完整位于滚动可视区；重放可再次启动。
- 聊天实际输入：手机 Enter 留下换行；退出再进入后恢复同一角色草稿。
- 报告实际操作：章节切换后回到正文顶部并聚焦标题；个人统计筛选更新计数。
- PDF 示例服务错误：提示保留，生成按钮恢复；系统返回关闭弹层并恢复导出按钮焦点。
- 模型实际操作：取消→继续编辑→系统返回→继续编辑→再次系统返回→放弃；表单保留、确认层正确、最终回到模型列表。

## 验证边界

浏览器验收使用示例数据。尚未在真实 iOS/Android 设备上验收软键盘、安全区、音频播放策略与长时间 GPU 表现，也未在本轮走真实 PDF 下载和完整后端跑团流程。大成功/大失败采用骰子实际 viewport 的局部坐标定位，瞬时特效仍需真机视觉复核。桌面主布局已检查，全部桌面交互没有逐项重跑。

系统 History API 无法删除前进栈条目；本实现会清理正常后退步骤，旧前进条目不会重新打开已经关闭的弹层。
