<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.me.galchat.mapper.VectorStoreCleanupMapper">

    <delete id="deleteChatHistoryByConversation">
        DELETE FROM public.chat_history_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, ',"characterId":', #{characterId}, '}'
        )::jsonb
    </delete>

    <delete id="deleteChatHistoryByConversationAfterEnd">
        DELETE FROM public.chat_history_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, ',"characterId":', #{characterId}, '}'
        )::jsonb
          AND (metadata::jsonb ->> 'endMessageId')::bigint &gt; #{thresholdEndMessageId}
    </delete>

    <delete id="deleteGroupTopicsAfterId">
        DELETE FROM public.group_topic_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, '}'
        )::jsonb
          AND (metadata::jsonb ->> 'groupTopicId')::bigint &gt; #{maxGroupTopicId}
    </delete>

    <delete id="deleteGroupTopicsByConversation">
        DELETE FROM public.group_topic_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"conversationId":', #{conversationId}, '}'
        )::jsonb
    </delete>

    <delete id="deleteGroupTopicsByConversationAfterEnd">
        DELETE FROM public.group_topic_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"conversationId":', #{conversationId}, '}'
        )::jsonb
          AND (metadata::jsonb ->> 'endSequence')::bigint &gt; #{thresholdEndSequence}
    </delete>

    <delete id="deleteWorldEventByUserWorldId">
        DELETE FROM public.world_event_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, '}'
        )::jsonb
    </delete>

    <delete id="deleteWorldEventByUserWorldIdAfterLogId">
        DELETE FROM public.world_event_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, '}'
        )::jsonb
          AND (metadata::jsonb ->> 'worldEventLogId')::bigint &gt; #{worldEventLogId}
    </delete>

    <delete id="deleteWorldEventByLogId">
        DELETE FROM public.world_event_vector_store
        WHERE metadata::jsonb @> CONCAT(
            '{"userWorldId":', #{userWorldId}, ',"worldEventLogId":', #{worldEventLogId}, '}'
        )::jsonb
    </delete>
</mapper>
